{
  "name": "gitinfo",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "bin": {
    "git-info": "dist/index.js"
  },
  "scripts": {
    "build": "tsc --build tsconfig.json --outDir dist --module commonjs --target es2020",
    "watch": "tsc --watch --outDir dist --module commonjs --target es2020",
    "dev": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "include": [
    "./typings-custom/**/*.ts"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.15.5",
    "typescript": "^5.0.2"
  },
  "map": {
    "child_process": "@node/child_process"
  }
}